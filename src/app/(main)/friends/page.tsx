"use client";

import EmptyState from "@/components/EmptyState";

const page = () => {
  return (
    <div className="hidden lg:block w-full h-full">
      <EmptyState />
    </div>
  );
};

export default page;
